Crie um novo agente em services/agents/$ARGUMENTS seguindo o padrão:
contrato Pydantic em packages/contracts, registro no Agent Registry (capabilities, tier permitido,
orçamento de tokens), integração com a cascata (sem chamada direta a LLM), testes e um caso de eval.
